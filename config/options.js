module.exports = {
    "deckName": "Maps",
    "mapSize": 500,            // map width and height in pixels
    "mapContextRadius": 0.002, // how much context to show around the primary feature, in degress
    "mapDetail": 17,           // integer 1-17
    "maxImageSize": 512,       // scale non-map images so both dimensions are within this limit
    "markerRadius": 8          // radius of marker on maps to indicate a place
}
